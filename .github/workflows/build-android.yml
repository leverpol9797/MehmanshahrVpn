name: Build and Release MehmanshahrVpn APKs

on:
  push:
    branches: [ "**" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Fetch native VPN core assets
        run: |
          mkdir -p android/app/src/main/jniLibs
          base='https://github.com/CluvexStudio/Aether/releases/download/v1.9.0'
          for spec in 'armeabi-v7a aether-android-armv7.tar.gz d49ee19423a33d905fb4fef3f163d2e3c88e5223940e0f03dbe6324bb2c7dcdb e93d13c270f6636195a6262ec642235d75b2e6a15fe9e5640978b5483407da6a' 'arm64-v8a aether-android-arm64.tar.gz a5a488b8cf05b3e83c28ca35cef78334130411c8df5314850e816b900e9d6cb9 892d4fa467b32dd447791e08f518238ceeb0ab16c702d302077851565d0b8829' 'x86_64 aether-android-x86_64.tar.gz 0c4dfcea54b5a39c0a3a52473d1fb9c2ff5c4ed92de5d240f84f18f54425f961 eb2bf174dbea1095a890f9e1f358dbcb5aa62fc00c039e8becc89f0f212e7327'; do
            set -- $spec; abi=$1; archive=$2; expected=$3; binary=$4
            tmp=$(mktemp -d); curl -L --fail "$base/$archive" -o "$tmp/$archive"
            echo "$expected  $tmp/$archive" | sha256sum -c -
            tar -xzf "$tmp/$archive" -C "$tmp"
            core=$(find "$tmp" -type f -name aether | head -1)
            test "$(sha256sum "$core" | cut -d' ' -f1)" = "$binary"
            mkdir -p "android/app/src/main/jniLibs/$abi"
            cp "$core" "android/app/src/main/jniLibs/$abi/libaether.so"
            rm -rf "$tmp"
          done
          
          NDK_BUILD=$(find $ANDROID_SDK_ROOT/ndk -name ndk-build | sort -V | tail -1)
          if [ -z "$NDK_BUILD" ]; then
            NDK_BUILD=$(find $ANDROID_HOME/ndk -name ndk-build | sort -V | tail -1)
          fi
          echo "Using ndk-build: $NDK_BUILD"

          work=$(mktemp -d)
          git clone --quiet --branch 2.16.0 --depth 1 --recurse-submodules https://github.com/heiher/hev-socks5-tunnel.git "$work/hev"
          "$NDK_BUILD" NDK_PROJECT_PATH="$work/hev" APP_BUILD_SCRIPT="$work/hev/Android.mk" NDK_APPLICATION_MK="$work/hev/Application.mk" 'APP_ABI=armeabi-v7a arm64-v8a x86_64' 'APP_CFLAGS=-O3 -DPKGNAME=hev/htproxy' NDK_LIBS_OUT="$work/libs" NDK_OUT="$work/obj" -j4
          for abi in armeabi-v7a arm64-v8a x86_64; do cp "$work/libs/$abi/libhev-socks5-tunnel.so" "android/app/src/main/jniLibs/$abi/"; done
          rm -rf "$work"

      - name: Run unit tests
        run: |
          cd android
          chmod +x gradlew
          ./gradlew testDebugUnitTest --console=plain
          # گزارش را به‌عنوان آرتیفکت نگه می‌داریم تا شکست‌ها قابل بررسی باشند
          find app/build/reports/tests -name '*.html' -print || true

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-report
          path: android/app/build/reports/tests/
          if-no-files-found: ignore

      - name: Build Android Debug APKs
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Prepare Release APK Files
        run: |
          mkdir -p release_apks
          find android/app/build/outputs/apk -type f -name '*.apk' -print
          apk_dir="android/app/build/outputs/apk/debug"
          for f in "$apk_dir"/*.apk; do
            [ -f "$f" ] || continue
            name=$(basename "$f")
            echo "Processing $name"
            if [[ "$name" == *"arm64-v8a"* ]]; then
              cp "$f" release_apks/MehmanshahrVpn-v2.1.1-ARM64-v8a.apk
            elif [[ "$name" == *"armeabi-v7a"* ]]; then
              cp "$f" release_apks/MehmanshahrVpn-v2.1.1-ARMv7.apk
            elif [[ "$name" == *"universal"* ]]; then
              cp "$f" release_apks/MehmanshahrVpn-v2.1.1-Universal.apk
            else
              cp "$f" "release_apks/$name"
            fi
          done
          ls -la release_apks
          test -f release_apks/MehmanshahrVpn-v2.1.1-ARM64-v8a.apk
          test -f release_apks/MehmanshahrVpn-v2.1.1-ARMv7.apk

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: MehmanshahrVpn-Release-APKs
          path: release_apks/*.apk

      # Only the default branch publishes. Every other branch still builds and
      # runs the tests, but a feature push must not replace the public release —
      # the step below deletes the existing tag first, so an unguarded trigger
      # means anyone with push access silently swaps the published APKs.
      - name: Publish to GitHub Releases
        if: github.ref == 'refs/heads/main'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete v2.1.1 --yes || true
          git tag -d v2.1.1 || true
          gh release create v2.1.1 release_apks/*.apk \
            --title "MehmanshahrVpn v2.1.1" \
            --notes "### MehmanshahrVpn Android Release
            - **ورود:** با لایسنس. شمارهٔ موبایل خود را برای ربات \`@MehmanshahrVpnBot\` در روبیکا بفرستید تا لایسنس صادر شود.
            - **توسعه دهنده:** ساخته شده توسط Hossein Akhlaghi
            
            #### فایل‌های دانلودی:
            - \`MehmanshahrVpn-v2.1.1-ARM64-v8a.apk\` : مخصوص گوشی‌های جدید (۶۴ بیتی)
            - \`MehmanshahrVpn-v2.1.1-ARMv7.apk\` : مخصوص گوشی‌های قدیمی‌تر (۳۲ بیتی)
            - \`MehmanshahrVpn-v2.1.1-Universal.apk\` : نسخه عمومی سازگار با تمام گوشی‌ها"
